services:
  homemediaserver:
    image: homemediaserver
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - ${MEDIA_CATALOG_SERVICE_PORT:8081}:8081
      - ${MEDIA_ACQUISITION_SERVICE_PORT:8082}:8082
      - ${MEDIA_STREAM_SERVICE_PORT:8083}:8083
